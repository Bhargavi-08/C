#include<stdio.h>
void main()
{
    int n;
    long int fact=1;
    printf("Enter n:");
    scanf("%d",&n);
    for( int i=1;i<=n;i++)
    {
        fact=fact*i;
    }
    printf("The factorial of a given number %d is %d",n,fact);
}
